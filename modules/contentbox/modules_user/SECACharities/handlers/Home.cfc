component{
        function index( event, rc, prc ){
             event.setView( "home/index" );
       }

       function MySecrets( event, rc, prc ){
             event.setView( "home/index" );
       }
}